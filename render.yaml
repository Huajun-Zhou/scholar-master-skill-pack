services:
  - type: web
    name: scholar-api
    runtime: python
    buildCommand: pip install -e .
    startCommand: cd api && uvicorn server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
